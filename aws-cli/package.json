{
  "name": "aws-cli-cheatsheet",
  "version": "1.0.0",
  "private": true,
  "description": "Offline AWS CLI v2 cheatsheet — static HTML, no build step.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "screenshots": "node scripts/screenshots.mjs",
    "serve": "npx --yes http-server -p 8080 -c-1 ."
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  },
  "license": "MIT"
}
